import { View, Text } from "react-native";
import MyStyles from "../../styles/MyStyles";

export default Home = () => {
    return(
        <View style={MyStyles.container}>
            <Text>Home</Text>
        </View>
    );
}